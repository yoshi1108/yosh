package uma.util

class UlogInner {
	def error(def obj){
		log.error(obj);
	}
	def warn(def obj){
		log.warn(obj);
	}
	def info(def obj){
		log.info(obj);
	}
	def debug(def obj){
		log.debug(obj);
	}
}
